import 'package:flutter/material.dart';

class AirpayScreen extends StatelessWidget{
  @override
  Widget build (BuildContext context){
    return Scaffold(
      appBar:AppBar(title: Text('Airpay')),
      body: Center(child: Text('Airpay'))
    );
  }
}